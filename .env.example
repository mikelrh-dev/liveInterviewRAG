# InterviewTTS Environment Variables
# Copy to .env and fill in values

# OpenRouter API (required as fallback)
OPENROUTER_API_KEY=your-api-key-here
LLM_MODEL=openrouter/owl-alpha
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=500

# Google AI API (optional — primary provider when set)
# Get your API key at https://aistudio.google.com/apikey
# GOOGLE_API_KEY=your-google-ai-key-here
# GOOGLE_MODEL=gemini-3.1-flash-lite

# Whisper settings
# Default `tiny` for ~4x faster CPU transcription; set to `base` for higher accuracy.
WHISPER_MODEL=tiny
WHISPER_DEVICE=cpu
WHISPER_COMPUTE_TYPE=int8

# TTS voice
TTS_VOICE=es-ES-AlvaroNeural

# CORS origins (comma-separated for production)
CORS_ORIGINS=http://localhost:8000,https://tudominio.com

# RAG settings
EMBEDDING_MODEL=all-MiniLM-L6-v2
RAG_TOP_K=3
CHUNK_SIZE=400
CHUNK_OVERLAP=50

# Server
HOST=0.0.0.0
PORT=8000

# Rate limiting
RATE_LIMIT_PER_MINUTE=10
MAX_AUDIO_DURATION=30
